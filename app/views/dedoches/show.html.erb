<div class="large-3 small-12 column">
  <%=image_tag @dedoche.image %>
</div>

<div class="large-9 small-12 column">
  <h3>
    <%= @dedoche.name %>
    (<%= @dedoche.stock %>)
  </h3>
  <p class="label movie-label radius mb1">$ <%= @dedoche.price %></p>

  <p><%= @dedoche.description %></p>
</div>

<%= link_to 'Back', dedoches_path %>
